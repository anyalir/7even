export function BoardPage() {
  return (
    <div>
      <h1 style={{ textTransform: "uppercase", letterSpacing: "0.1em", marginBottom: 16 }}>
        TASKS BOARD
      </h1>
      <div className="lcars-card">
        <p style={{ color: "var(--text-muted)" }}>Coming soon — Kanban board with task cards</p>
      </div>
    </div>
  );
}
