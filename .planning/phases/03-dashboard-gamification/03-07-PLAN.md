---
phase: 03-dashboard-gamification
plan: 07
type: execute
wave: 4
depends_on:
  - "03-04"
  - "03-05"
  - "03-06"
files_modified:
  - src/dashboard/app/pages/AchievementsPage.tsx
  - src/dashboard/app/components/BadgeCard.tsx
  - src/dashboard/app/components/AchievementTimeline.tsx
  - src/dashboard/app/components/SidebarBadgeIndicator.tsx
  - src/dashboard/app/layouts/AppLayout.tsx
autonomous: false
requirements:
  - GAME-04

must_haves:
  truths:
    - "Achieved Objectives page shows hall-of-fame with badges per person"
    - "Achievement timeline shows when badges were earned with associated O/KRs"
    - "Sidebar pulses when new badges earned (no toast interruption)"
    - "Dashboard has cohesive LCARS productivity game aesthetic throughout"
  artifacts:
    - path: "src/dashboard/app/pages/AchievementsPage.tsx"
      provides: "Hall of fame with badge showcase and timeline"
    - path: "src/dashboard/app/components/BadgeCard.tsx"
      provides: "Individual badge display card"
    - path: "src/dashboard/app/components/AchievementTimeline.tsx"
      provides: "Chronological badge earning timeline"
    - path: "src/dashboard/app/components/SidebarBadgeIndicator.tsx"
      provides: "Pulsing indicator for new badges"
  key_links:
    - from: "src/dashboard/app/pages/AchievementsPage.tsx"
      to: "/api/badges"
      via: "fetch earned and available badges"
      pattern: "fetch.*api/badges"
    - from: "src/dashboard/app/components/SidebarBadgeIndicator.tsx"
      to: "/api/badges"
      via: "check for newly earned badges"
      pattern: "fetch.*api/badges"
---

<objective>
Build the Achieved Objectives / hall-of-fame page, badge display, achievement timeline, and sidebar notification indicator. Final visual checkpoint.

Purpose: Completes the gamification layer with the achievements page and integrates badge notifications into the sidebar.
Output: Working achievements page with badge showcase, timeline, and pulsing sidebar indicator.
</objective>

<execution_context>
@/Users/anyarudolph/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/anyarudolph/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-gamification/03-RESEARCH.md
@.planning/phases/03-dashboard-gamification/03-03-SUMMARY.md
@.planning/phases/03-dashboard-gamification/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build achievements page with badge cards and timeline</name>
  <files>src/dashboard/app/pages/AchievementsPage.tsx, src/dashboard/app/components/BadgeCard.tsx, src/dashboard/app/components/AchievementTimeline.tsx</files>
  <action>
**AchievementsPage.tsx (per user decisions: "hall-of-fame style page with achievements and badges associated to people"):**
- Fetch `/api/badges` for earned badges, available badges, and project state
- Two sections:
  1. Per-person badge showcase: group earned badges by person (from task assignees who triggered the badge context). Show person name/email, their earned badges as BadgeCards
  2. Achievement timeline: chronological view of all badge earnings
- Also shows achieved objectives with their associated KRs in a "completed" showcase section
- LCARS framing: section headers with 3px bars, warm pastel accents
- Progress bars showing overall completion (objectives achieved / total, KRs achieved / total)

**BadgeCard.tsx:**
- Badge icon (emoji), name, description
- Earned state: full color vs greyed out (for "available but not earned")
- Earned date displayed below
- LCARS dot decoration in top-left corner
- Sharp edges, surface background

**AchievementTimeline.tsx:**
- Vertical timeline with date markers
- Each entry: badge icon + name + "earned by {person}" + associated O/KR names
- 3px vertical bar as timeline spine
- Entries alternate left/right for visual interest
- Most recent at top
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Achievements page renders per-person badge showcase and chronological timeline; badges display earned/unearned states</done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar badge indicator and final LCARS polish</name>
  <files>src/dashboard/app/components/SidebarBadgeIndicator.tsx, src/dashboard/app/layouts/AppLayout.tsx</files>
  <action>
**SidebarBadgeIndicator.tsx (per user decision: "sidebar indicator that pulses until clicked. No interrupting toasts"):**
- Fetches `/api/badges` to check for newly earned badges (earned since last viewed)
- Track "last viewed" timestamp in localStorage
- When new badges exist: pulse animation on a small badge icon in sidebar
- Click opens a dropdown showing newly earned badges
- After clicking, update "last viewed" timestamp, stop pulsing
- CSS pulse animation using @keyframes (subtle glow, not flashy)

**AppLayout.tsx updates:**
- Integrate SidebarBadgeIndicator into sidebar below nav links
- Add VelocityChart in `compact` mode to sidebar bottom (per user decision: "persistent widget summary in sidebar/header")
- Final LCARS polish pass:
  - Ensure 3px decorative elements are consistent across all pages
  - Verify dark-on-light only applies to detail panels, not nav/boards
  - Check color propagation: objective colors → KR shades consistent everywhere
  - Ensure no rounded corners leaked through
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar shows pulsing badge indicator for new achievements; velocity widget in sidebar; LCARS styling consistent across all pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <files>src/dashboard/app/pages/AchievementsPage.tsx</files>
  <action>
  Human verifies the complete dashboard visually.
  What was built: Complete dashboard with all pages — Project Timeline (Gantt), Tasks Board (kanban), Achieved Objectives (hall of fame), Analytics (charts). LCARS-inspired design with gamification elements.
  How to verify:
    1. Run `npx 7 dashboard` — browser should open to dashboard
    2. Check Timeline page: Gantt chart with expandable objectives, KR bars, time axis
    3. Check Board page: KR-lane kanban, task cards with SP color blocks, click task for detail panel
    4. Check Achievements page: badge showcase per person, achievement timeline
    5. Check Analytics page: burndown chart (toggle KR/objective), velocity bars, commit metrics
    6. Verify LCARS aesthetic: dark background, warm pastels, no rounded corners, 3px decorative bars
    7. Verify sidebar: navigation, velocity widget, badge pulse indicator
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>User approves dashboard visual design and functionality across all pages</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit
npx 7 dashboard
# Visual verification of all pages and LCARS aesthetic
```
</verification>

<success_criteria>
- Achievements page shows hall of fame with per-person badges and timeline
- Sidebar badge indicator pulses for new achievements, no toasts
- Velocity widget persists in sidebar
- All LCARS design elements consistent across pages
- Dashboard fully functional end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-gamification/03-07-SUMMARY.md`
</output>
