---
phase: 03-dashboard-gamification
plan: 06
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - src/dashboard/app/pages/AnalyticsPage.tsx
  - src/dashboard/app/components/BurndownChart.tsx
  - src/dashboard/app/components/VelocityChart.tsx
  - src/dashboard/app/components/CommitMetricsChart.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-06
  - DASH-09

must_haves:
  truths:
    - "Burndown chart shows ideal vs actual lines with per-KR and per-objective toggle"
    - "Velocity chart shows SP completed per time window with ETA projection"
    - "Commit frequency and PR metrics are visualized as charts"
  artifacts:
    - path: "src/dashboard/app/components/BurndownChart.tsx"
      provides: "Recharts burndown with ideal/actual lines"
    - path: "src/dashboard/app/components/VelocityChart.tsx"
      provides: "Velocity bar chart with ETA projection"
    - path: "src/dashboard/app/components/CommitMetricsChart.tsx"
      provides: "Commit frequency and PR weight charts"
    - path: "src/dashboard/app/pages/AnalyticsPage.tsx"
      provides: "Drill-down analytics page"
  key_links:
    - from: "src/dashboard/app/components/BurndownChart.tsx"
      to: "/api/metrics/burndown"
      via: "fetch burndown data"
      pattern: "fetch.*api/metrics/burndown"
    - from: "src/dashboard/app/components/VelocityChart.tsx"
      to: "/api/metrics/velocity"
      via: "fetch velocity data"
      pattern: "fetch.*api/metrics/velocity"
---

<objective>
Build burndown, velocity, and commit metric chart components and the Analytics drill-down page.

Purpose: Chart visualizations give users insight into project progress, pace, and development activity.
Output: Recharts-based interactive charts on a dedicated analytics page.
</objective>

<execution_context>
@/Users/anyarudolph/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/anyarudolph/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-gamification/03-RESEARCH.md
@.planning/phases/03-dashboard-gamification/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build burndown and velocity chart components</name>
  <files>src/dashboard/app/components/BurndownChart.tsx, src/dashboard/app/components/VelocityChart.tsx</files>
  <action>
**BurndownChart.tsx (DASH-05, per user decisions):**
- Recharts AreaChart with two lines: ideal (dashed) and actual (solid fill)
- Toggle between per-KR and per-objective view (dropdown selector)
- Per-KR: fetch `/api/metrics/burndown/:krId`
- Per-objective: aggregate all KR burndowns
- Interactive: hover tooltips showing date, remaining SP, ideal SP
- Zoom/pan on x-axis (use Recharts' referenceArea for brush zoom)
- LCARS styling: sharp edges, warm pastel line colors, dark background
- X-axis: dates. Y-axis: story points remaining.

**VelocityChart.tsx (DASH-06, per user decisions):**
- Recharts BarChart showing completed SP per velocity window
- Overlay line: rolling average
- ETA projection displayed as annotation: "Est. completion: {date}" with confidence badge
- Fetch `/api/metrics/velocity`
- Interactive tooltips showing window dates, SP completed, task count
- Also rendered as persistent mini-widget in sidebar (via props: `compact` mode)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Burndown chart renders ideal vs actual with KR/objective toggle; velocity chart shows SP bars with ETA projection</done>
</task>

<task type="auto">
  <name>Task 2: Build commit metrics chart and analytics page</name>
  <files>src/dashboard/app/components/CommitMetricsChart.tsx, src/dashboard/app/pages/AnalyticsPage.tsx</files>
  <action>
**CommitMetricsChart.tsx (DASH-09):**
- Two sub-charts:
  1. Commit frequency: Recharts BarChart — commits per day/week grouped by task
  2. PR/commit weight: Recharts BarChart — additions vs deletions (stacked bars)
- Fetch from `/api/metrics/commits/:taskId` or aggregate endpoint
- Filter by task, KR, or objective scope
- LCARS-styled with warm pastel bars

**AnalyticsPage.tsx (per user decision: "dedicated drill-down analytics page"):**
- Page layout with LCARS section framing
- Burndown section: BurndownChart with KR/objective selector
- Velocity section: VelocityChart full-size view
- Commit metrics section: CommitMetricsChart with scope filter
- Each section has 3px decorative header bar
- Scope selector at top: filter all charts by objective or KR
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Analytics page renders all three chart types with scope filtering and LCARS framing</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit
ls src/dashboard/app/components/*Chart.tsx src/dashboard/app/pages/AnalyticsPage.tsx
```
</verification>

<success_criteria>
- Burndown chart: ideal vs actual, per-KR and per-objective toggle, interactive
- Velocity chart: SP bars with rolling average and ETA projection
- Commit metrics: frequency and weight visualizations
- All charts render on analytics page with scope filtering
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-gamification/03-06-SUMMARY.md`
</output>
