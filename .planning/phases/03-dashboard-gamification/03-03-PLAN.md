---
phase: 03-dashboard-gamification
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/dashboard/vite.config.ts
  - src/dashboard/index.html
  - src/dashboard/app/main.tsx
  - src/dashboard/app/router.tsx
  - src/dashboard/app/styles/theme.css
  - src/dashboard/app/styles/lcars.css
  - src/dashboard/app/layouts/AppLayout.tsx
  - src/dashboard/app/components/LcarsSidebar.tsx
  - src/dashboard/api/server.ts
  - src/dashboard/api/routes/objectives.ts
  - src/dashboard/api/routes/tasks.ts
  - src/dashboard/api/routes/metrics.ts
  - src/dashboard/api/routes/badges.ts
  - src/cli/commands/dashboard.ts
  - package.json
autonomous: true
requirements:
  - DASH-01
  - DASH-03
  - GAME-04

must_haves:
  truths:
    - "User can run npx 7 dashboard and a browser opens with the dashboard"
    - "Dashboard has LCARS-inspired pastel-on-dark theme with sharp edges"
    - "Left sidebar navigation with LCARS blocky colored surfaces links to all pages"
    - "API serves objective, task, metrics, and badge data from .7even/"
  artifacts:
    - path: "src/dashboard/vite.config.ts"
      provides: "Vite config with React plugin and API proxy"
    - path: "src/dashboard/app/styles/theme.css"
      provides: "CSS custom properties for LCARS palette"
      contains: "border-radius: 0"
    - path: "src/dashboard/app/layouts/AppLayout.tsx"
      provides: "Main layout with sidebar and content area"
    - path: "src/dashboard/api/server.ts"
      provides: "Hono API server with all routes"
    - path: "src/cli/commands/dashboard.ts"
      provides: "CLI command to launch dashboard"
  key_links:
    - from: "src/dashboard/api/server.ts"
      to: "src/core/storage.ts"
      via: "reads .7even/ data"
      pattern: "readIndex|readItem"
    - from: "src/dashboard/app/router.tsx"
      to: "src/dashboard/app/pages/"
      via: "React Router routes"
      pattern: "createBrowserRouter"
    - from: "src/cli/commands/dashboard.ts"
      to: "src/dashboard/vite.config.ts"
      via: "spawns Vite dev server"
      pattern: "createServer"
---

<objective>
Scaffold the dashboard: Vite + React app, Hono API server, LCARS theme system, sidebar layout, and `npx 7 dashboard` CLI command.

Purpose: This is the foundation all dashboard pages build on — routing, API, theme, and layout.
Output: Working dashboard shell with LCARS styling, sidebar nav, API endpoints, and placeholder pages.
</objective>

<execution_context>
@/Users/anyarudolph/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/anyarudolph/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-gamification/03-RESEARCH.md
@.planning/phases/03-dashboard-gamification/03-01-SUMMARY.md
@.planning/phases/03-dashboard-gamification/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 metrics -->
From src/metrics/velocity.ts: `calculateVelocity(doneTasks, windowDays?)`
From src/metrics/burndown.ts: `computeBurndown(tasks, startDate?)`
From src/metrics/projection.ts: `projectEta(remainingSp, velocityWindows)`
From src/metrics/gantt.ts: `computeGanttBars(items)`
From src/metrics/git-metrics.ts: `getCommitMetrics(taskId)`, `getPrMetrics(taskId)`

<!-- From Plan 02 badges -->
From src/badges/checker.ts: `buildProjectState(sevenDir)`, `checkBadges(state, badges, earned)`, `loadEarnedBadges(sevenDir)`
From src/badges/builtins/index.ts: `builtinBadges`
From src/badges/loader.ts: `loadCustomBadges(sevenDir)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create Vite config, LCARS theme, and API server</name>
  <files>package.json, src/dashboard/vite.config.ts, src/dashboard/index.html, src/dashboard/app/main.tsx, src/dashboard/app/styles/theme.css, src/dashboard/app/styles/lcars.css, src/dashboard/api/server.ts, src/dashboard/api/routes/objectives.ts, src/dashboard/api/routes/tasks.ts, src/dashboard/api/routes/metrics.ts, src/dashboard/api/routes/badges.ts</files>
  <action>
**Install:** `npm install react react-dom react-router recharts hono date-fns @tanstack/react-table && npm install -D @types/react @types/react-dom @vitejs/plugin-react`

**vite.config.ts:** Vite 6 config with `@vitejs/plugin-react`, root set to `src/dashboard/`, server proxy `/api` to Hono server on port 7778.

**index.html:** Minimal HTML with `<div id="root">`, loads `/app/main.tsx`.

**main.tsx:** React 19 createRoot, renders RouterProvider.

**theme.css — LCARS palette (per user decisions):**
- `--bg-primary: #0a0a0f` (near-black)
- `--bg-surface: #14141f`
- `--text-primary: #e8e0d4` (warm off-white)
- 6 objective colors using oklch() warm pastels (coral, amber, sage, blue, lavender, rose)
- `* { border-radius: 0 !important; }` — no rounded corners ANYWHERE
- Modern sans-serif font: `font-family: 'Inter', system-ui, sans-serif`
- `text-transform: uppercase` for buttons
- 3px bar and dot decorative classes: `.lcars-bar-h`, `.lcars-bar-v`, `.lcars-dot`

**lcars.css — LCARS decorative system:**
- Full framing: page borders, section frames, card corners, dividers
- 3px wide bars and 3x3px squares as signature elements
- Decorations aligned to top-left padding of elements (off-center)
- Dark-on-light classes for detail panels: `.panel-detail { background: var(--text-primary); color: var(--bg-primary); }`
- Light-on-dark for nav/boards (default)

**API server (server.ts):**
- Hono app with routes mounted
- `/api/objectives` — returns all objectives with nested KRs and tasks
- `/api/tasks` — returns all tasks with parent info
- `/api/metrics/velocity` — calls calculateVelocity, returns JSON
- `/api/metrics/burndown/:krId` — calls computeBurndown for KR's tasks
- `/api/metrics/gantt` — calls computeGanttBars
- `/api/badges` — returns earned badges + available badges
- Reads `sevenDir` from CWD + `/.7even`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20; echo "---"; ls src/dashboard/api/routes/ src/dashboard/app/styles/</automated>
  </verify>
  <done>Vite config created, LCARS theme CSS defined, Hono API routes serve data from .7even/, all TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create AppLayout with LCARS sidebar and router, add CLI dashboard command</name>
  <files>src/dashboard/app/router.tsx, src/dashboard/app/layouts/AppLayout.tsx, src/dashboard/app/components/LcarsSidebar.tsx, src/dashboard/app/pages/TimelinePage.tsx, src/dashboard/app/pages/BoardPage.tsx, src/dashboard/app/pages/AchievementsPage.tsx, src/cli/commands/dashboard.ts</files>
  <action>
**router.tsx:** React Router 7 with createBrowserRouter:
- `/` → redirect to `/timeline`
- `/timeline` → TimelinePage (Project Timeline)
- `/board` → BoardPage (Tasks Board)
- `/achievements` → AchievementsPage (Achieved Objectives)
- `/analytics` → AnalyticsPage (velocity/ETA drill-down — placeholder)
- All wrapped in AppLayout

**AppLayout.tsx:**
- CSS grid: sidebar (280px fixed) + main content
- LCARS page border framing (3px bars around edges)
- Achievement notification indicator in sidebar (pulses when new badges — per user decision: NO toasts)

**LcarsSidebar.tsx:**
- LCARS blocky colored navigation surfaces (per user decision)
- NavLinks for Timeline, Board, Achievements, Analytics
- Active state with brighter color variant
- Velocity/ETA persistent widget summary at bottom (per user decision: "persistent widget summary in sidebar/header")
- 3px decorative bars between sections
- All caps labels

**Placeholder pages:** Each page exports a component with page title and "Coming soon" — filled in by plans 04-06.

**dashboard.ts CLI command:**
- `npx 7 dashboard [--port 7777]`
- Spawns Hono API server on port 7778
- Spawns Vite dev server on port 7777 (or user-specified) with `open: true`
- Console output: `Dashboard: http://localhost:7777`
- Register command in main CLI entry point
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard launches with `npx 7 dashboard`, browser opens, LCARS sidebar visible with nav links, API endpoints respond with data</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles
npx tsc --noEmit

# Dashboard files exist
ls src/dashboard/app/pages/ src/dashboard/api/routes/ src/dashboard/app/layouts/

# CLI command registered
npx tsx src/cli/index.ts dashboard --help
```
</verification>

<success_criteria>
- `npx 7 dashboard` spawns Vite + API server and opens browser
- LCARS theme: near-black background, warm pastels, no rounded corners, 3px bars
- Left sidebar with blocky nav surfaces linking to all pages
- API returns real data from .7even/ for objectives, tasks, metrics, badges
- Placeholder pages render for all routes
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-gamification/03-03-SUMMARY.md`
</output>
