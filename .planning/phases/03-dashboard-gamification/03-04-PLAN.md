---
phase: 03-dashboard-gamification
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - src/dashboard/app/pages/BoardPage.tsx
  - src/dashboard/app/components/KanbanLane.tsx
  - src/dashboard/app/components/TaskCard.tsx
  - src/dashboard/app/components/TaskDetailPanel.tsx
  - src/dashboard/app/components/AssigneeView.tsx
  - src/dashboard/app/hooks/useApi.ts
autonomous: true
requirements:
  - DASH-02
  - DASH-04
  - DASH-08
  - DASH-10

must_haves:
  truths:
    - "Tasks Board shows KR-lane kanban with tasks grouped by status within each KR"
    - "Task cards have color block on left side scaled by SP estimate"
    - "Clicking a task opens slide-in detail panel with comments and git commits"
    - "Assignee view shows who is working on what"
    - "Achieved KRs are collapsed in the board view"
  artifacts:
    - path: "src/dashboard/app/pages/BoardPage.tsx"
      provides: "KR-lane kanban board page"
    - path: "src/dashboard/app/components/TaskCard.tsx"
      provides: "Task card with SP color block"
    - path: "src/dashboard/app/components/TaskDetailPanel.tsx"
      provides: "Slide-in panel with comments and git history"
    - path: "src/dashboard/app/components/AssigneeView.tsx"
      provides: "Per-person task list view"
  key_links:
    - from: "src/dashboard/app/pages/BoardPage.tsx"
      to: "/api/tasks"
      via: "fetch tasks grouped by KR"
      pattern: "fetch.*api/tasks"
    - from: "src/dashboard/app/components/TaskDetailPanel.tsx"
      to: "/api/metrics"
      via: "fetch git commits for task"
      pattern: "fetch.*api/metrics"
---

<objective>
Build the Tasks Board page: KR-lane kanban view, task cards with SP visualization, slide-in task detail panel, and assignee view.

Purpose: The kanban board is the primary task management view, showing all work organized by KR and status.
Output: Fully functional Tasks Board page with detail panel and assignee grouping.
</objective>

<execution_context>
@/Users/anyarudolph/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/anyarudolph/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-gamification/03-RESEARCH.md
@.planning/phases/03-dashboard-gamification/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build KR-lane kanban board with task cards</name>
  <files>src/dashboard/app/pages/BoardPage.tsx, src/dashboard/app/components/KanbanLane.tsx, src/dashboard/app/components/TaskCard.tsx, src/dashboard/app/hooks/useApi.ts</files>
  <action>
**useApi.ts:** Simple fetch hook — `useApi<T>(url: string): { data: T | null, loading: boolean, error: string | null, refetch: () => void }`. Uses useState + useEffect. No heavy state library needed (read-only dashboard per research).

**BoardPage.tsx:**
- Fetch `/api/tasks` and `/api/objectives` on mount
- Group tasks by parent KR ID
- For each KR, render a KanbanLane
- Toggle between "by KR" and "by assignee" views (tab at top)
- Achieved KRs collapsed by default with expand toggle (per user decision)

**KanbanLane.tsx:**
- Header: KR name with objective's color shade (KR shade derived from parent objective per user decision)
- 3 columns: to-do, in-progress, done
- CSS grid layout within lane
- 3px decorative bar under header

**TaskCard.tsx (per locked user decisions):**
- Left edge: color block whose WIDTH scales with SP estimate (wider = more complex). Not heatmap colors — spatial size.
- Card background: `--bg-surface`
- Task name, assignee avatar/initials
- Remaining estimate in bottom-left corner with 3px margins
- onClick: opens TaskDetailPanel
- Sharp edges (no border-radius — enforced globally)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Board page renders KR lanes with task cards; cards show SP-scaled color blocks; achieved KRs are collapsed</done>
</task>

<task type="auto">
  <name>Task 2: Build task detail slide-in panel and assignee view</name>
  <files>src/dashboard/app/components/TaskDetailPanel.tsx, src/dashboard/app/components/AssigneeView.tsx</files>
  <action>
**TaskDetailPanel.tsx (per locked user decisions):**
- Slides in from right side of screen
- LCARS-style vertical divider on left edge of panel (3px bar) — doubles as task quick-nav for all tasks in that KR (clickable task names along the divider)
- Dark-on-light mode: light background, dark text (per user decision for detail panels)
- Content sections:
  - Task name and status
  - Estimation history table (date, SP, estimator)
  - Inline comments (from task JSON comments array) — author, date, text
  - Git commit history: fetch from `/api/metrics/commits/:taskId`, show hash (truncated), date, message
  - Acceptance criteria list (if present)
- Close button (X) in top-right
- Escape key closes panel

**AssigneeView.tsx (DASH-10):**
- Fetch tasks, group by `assignee.email`
- For each person: display name/email, count of active tasks, list of task cards
- Handles unassigned tasks as separate group
- Reuses TaskCard component
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Task detail panel slides in with comments, git history, and KR quick-nav; assignee view groups tasks by person</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit
ls src/dashboard/app/components/TaskDetailPanel.tsx src/dashboard/app/components/AssigneeView.tsx
```
</verification>

<success_criteria>
- Tasks Board renders KR-lane kanban with status columns
- Task cards display SP-scaled color blocks on left edge
- Clicking task opens slide-in detail panel with dark-on-light styling
- Detail panel shows comments, git commits, estimation history
- Assignee view groups tasks by person
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-gamification/03-04-SUMMARY.md`
</output>
